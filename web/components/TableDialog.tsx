'use client';
import { useState, type DragEvent, type KeyboardEvent } from 'react';
import { X, Plus, Trash2, KeyRound, GripVertical } from 'lucide-react';
import { COLORS, DEFAULT_GENERATION_PATTERN, generationPatternError, generationPreview, newColumn, Project, Table, TYPES, validateProject } from '../lib/schema';

export default function TableDialog({table, project, onClose, onSave}: {table: Table; project: Project; onClose:()=>void; onSave:(t:Table)=>void}) {
  const [draft,setDraft]=useState<Table>(structuredClone(table)); const [error,setError]=useState(''); const [dragged,setDragged]=useState<string | null>(null);
  const updateColumn=(id:string, patch:Partial<Table['columns'][number]>)=>setDraft(t=>({...t,columns:t.columns.map(c=>c.id===id?{...c,...patch}:c)}));
  const moveColumn=(from:string,to:string)=>setDraft(t=>{const columns=[...t.columns];const fromIndex=columns.findIndex(c=>c.id===from),toIndex=columns.findIndex(c=>c.id===to);if(fromIndex<0||toIndex<0||fromIndex===toIndex)return t;const [column]=columns.splice(fromIndex,1);columns.splice(toIndex,0,column);return {...t,columns};});
  const onDrop=(event:DragEvent, id:string)=>{event.preventDefault();if(dragged)moveColumn(dragged,id);setDragged(null);};
  const moveWithKeyboard=(event:KeyboardEvent, id:string)=>{if(event.key!=='ArrowUp'&&event.key!=='ArrowDown')return;event.preventDefault();const index=draft.columns.findIndex(c=>c.id===id),target=draft.columns[index+(event.key==='ArrowUp'?-1:1)];if(target)moveColumn(id,target.id);};
  function save(){try{const p={...project,tables:project.tables.some(t=>t.id===draft.id)?project.tables.map(t=>t.id===draft.id?draft:t):[...project.tables,draft]};const validated=validateProject(p);onSave(validated.tables.find(t=>t.id===draft.id)!);}catch(e){setError((e as Error).message);}}
  return <div className="modal-backdrop"><section className="modal table-modal" role="dialog" aria-modal="true" aria-labelledby="table-dialog-title" onKeyDown={e=>{if(e.key==='Escape')onClose();}}>
    <header className="modal-header"><div><span className="eyebrow">TABLE DESIGNER</span><h2 id="table-dialog-title">{project.tables.some(t=>t.id===draft.id)?'Edit tabel':'Tambah tabel'}</h2></div><button className="icon-button" aria-label="Tutup editor tabel" onClick={onClose}><X size={20}/></button></header>
    <div className="modal-body table-modal-body"><div className="table-editor-sticky"><div className="table-basics"><label>Nama tabel<input autoFocus aria-label="Nama tabel" value={draft.name} onChange={e=>setDraft({...draft,name:e.target.value})} placeholder="contoh: customers" maxLength={63}/></label><div><label>Warna tabel</label><div className="color-options">{COLORS.map(c=><button key={c} aria-label={`Warna ${c}`} className={draft.color===c?'chosen':''} style={{background:c}} onClick={()=>setDraft({...draft,color:c})}/>)}</div></div></div><div className="section-heading"><h3>Kolom <span>{draft.columns.length}</span></h3><span>PK = primary key · NN = not null · UQ = unique</span></div><div className="column-editor column-editor-head"><span/><span>Nama teknis</span><span>Nama tampilan</span><span>Tipe data</span><span>Deskripsi kolom</span><span>PK</span><span>NN</span><span>UQ</span><span>Sistem</span><span>Default value</span><span/></div></div>
      <div className="column-editor column-editor-rows">{draft.columns.map((c,index)=><div key={c.id} className="column-editor-group" onDragOver={e=>e.preventDefault()} onDrop={e=>onDrop(e,c.id)}><div className={`column-editor-row ${dragged===c.id?'dragging-column':''}`}><button type="button" className="column-drag-handle" draggable aria-label={`Ubah urutan kolom ${index+1}`} title="Tarik untuk mengubah urutan; gunakan panah atas/bawah dengan keyboard" onDragStart={()=>setDragged(c.id)} onDragEnd={()=>setDragged(null)} onKeyDown={e=>moveWithKeyboard(e,c.id)}><GripVertical size={15}/></button><input aria-label={`Nama kolom ${index+1}`} value={c.name} onChange={e=>updateColumn(c.id,{name:e.target.value})}/><input aria-label={`Nama tampilan kolom ${index+1}`} maxLength={120} value={c.displayName} placeholder="Contoh: Nomor Faktur" onChange={e=>updateColumn(c.id,{displayName:e.target.value})}/><select aria-label={`Tipe kolom ${index+1}`} value={c.type} onChange={e=>updateColumn(c.id,{type:e.target.value as typeof c.type})}>{TYPES.map(t=><option key={t}>{t}</option>)}</select><input aria-label={`Deskripsi kolom ${index+1}`} maxLength={2000} value={c.description} placeholder="Tujuan atau penggunaan kolom" onChange={e=>updateColumn(c.id,{description:e.target.value})}/><input type="checkbox" aria-label={`Primary key ${index+1}`} checked={c.primaryKey} onChange={e=>updateColumn(c.id,{primaryKey:e.target.checked,...(e.target.checked?{nullable:false}:{})})}/><input type="checkbox" aria-label={`Not null ${index+1}`} checked={!c.nullable} disabled={c.primaryKey} onChange={e=>updateColumn(c.id,{nullable:!e.target.checked})}/><input type="checkbox" aria-label={`Unique ${index+1}`} checked={c.unique} onChange={e=>updateColumn(c.id,{unique:e.target.checked})}/><input type="checkbox" aria-label={`Dihasilkan otomatis oleh sistem ${index+1}`} checked={c.systemGenerated} onChange={e=>updateColumn(c.id,{systemGenerated:e.target.checked,...(e.target.checked&&!c.generationPattern?{generationPattern:DEFAULT_GENERATION_PATTERN}:{})})}/><input aria-label={`Default kolom ${index+1}`} value={c.defaultValue} placeholder="—" onChange={e=>updateColumn(c.id,{defaultValue:e.target.value})}/><button className="icon-button danger" aria-label={`Hapus kolom ${index+1}`} disabled={draft.columns.length===1||project.relations.some(r=>r.fromColumn===c.id||r.toColumn===c.id)} title="Hapus relasi terkait terlebih dahulu" onClick={()=>setDraft({...draft,columns:draft.columns.filter(x=>x.id!==c.id)})}><Trash2 size={15}/></button></div>{c.systemGenerated&&<div className="generation-settings compact-generation"><label>Format Nilai Otomatis<input aria-label={`Format nilai otomatis ${index+1}`} maxLength={200} value={c.generationPattern} placeholder={DEFAULT_GENERATION_PATTERN} onChange={e=>updateColumn(c.id,{generationPattern:e.target.value})}/></label>{generationPatternError(c.generationPattern)?<p className="generation-error" role="status">{generationPatternError(c.generationPattern)}</p>:<p className="generation-example">Contoh hasil: <code>{generationPreview(c.generationPattern)}</code><span>Gunakan token {'{SEQ}'}, {'{DD}'}, {'{MM}'}, dan {'{YYYY}'}.</span></p>}</div>}</div>)}</div>
      <button className="add-column" disabled={draft.columns.length>=100} onClick={()=>{let n=draft.columns.length+1;while(draft.columns.some(c=>c.name===`column_${n}`))n++;setDraft({...draft,columns:[...draft.columns,newColumn(`column_${n}`,'varchar(255)')]});}}><Plus size={15}/> Tambah kolom</button><label className="notes-label">Catatan <span>opsional</span><textarea aria-label="Catatan tabel" rows={2} maxLength={2000} value={draft.note} onChange={e=>setDraft({...draft,note:e.target.value})} placeholder="Jelaskan kegunaan tabel ini…"/></label><p className="form-hint"><KeyRound size={13}/> Tarik titik dari kolom PK / UNIQUE ke kolom FK di canvas. Default teks ditulis tanpa tanda kutip.</p>{error&&<div className="form-error" role="alert">{error}</div>}
    </div><footer className="modal-footer"><button onClick={onClose}>Batal</button><button className="primary" onClick={save}>Simpan tabel</button></footer>
  </section></div>;
}
